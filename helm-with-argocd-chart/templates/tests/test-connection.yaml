apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-with-argocd-chart.fullname" . }}-test-connection"
  labels:
    {{- include "helm-with-argocd-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-with-argocd-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
