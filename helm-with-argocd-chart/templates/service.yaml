apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-with-argocd-chart.fullname" . }}
  labels:
    {{- include "helm-with-argocd-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-with-argocd-chart.selectorLabels" . | nindent 4 }}
